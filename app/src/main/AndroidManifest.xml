<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.bignerdranch.com.fourredd" >

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="android.bignerdranch.com.fourredd.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.bignerdranch.com.fourredd.permission.C2D_MESSAGE" />
    <uses-permission android:name="android.bignerdranch.com.fourredd.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.bignerdranch.com.fourredd.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Four"
            android:label="@string/title_activity_four" />
        <activity
            android:name=".Redd"
            android:label="@string/title_activity_redd" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".Register"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".CreateThread"
            android:label="@string/title_activity_create_thread" />
        <activity
            android:name=".Comments"
            android:label="@string/title_activity_comments" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".FourMapActivity"
            android:label="@string/title_activity_four_map" />
        <activity
            android:name=".TestFourMapActivity"
            android:label="TestFourMap" />
        <activity
            android:name=".AddComment"
            android:label="@string/title_activity_add_comment" >
        </activity>
    </application>

</manifest>
